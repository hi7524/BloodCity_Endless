using UnityEngine;

// 회전체가 수평을 유지하도록 함
public class SkillObjPoint : MonoBehaviour
{
    public float speed = 15;
    private void Start()
    {
        
    }
    private void Update()
    {
        /*transform.rotation = Quaternion.LookRotation(new Vector3(0, 0, 0));*/
        

    }
}
